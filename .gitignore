node_modules/
dist/
.output/
.tanstack/
.nitro/
.vinxi/
.env
.env.local
.env.prod
.env.*.local
*.log

# local SQLite (the data dir / Fly volume lives outside the repo anyway)
*.db
*.db-wal
*.db-shm

# python bytecode cache (regenerated by CPython; scripts/*.py are the source)
__pycache__/
*.pyc

# local runtime artifacts (daemon shim, downloaded delivery prompts, scratch task files)
.loopany/
/loopany/

# development docs
.lavish/

# local MCP server config (may carry machine-specific paths/secrets)
.mcp.json

# verification proof output (screenshots/video), never committed
evidence/
/.playwright-cli
