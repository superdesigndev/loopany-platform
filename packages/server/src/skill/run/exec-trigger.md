[loop run · {{name}}]

Run now. Read your task file at {{taskFile}} (create it from your Spec if it is missing), follow its `## Spec`, do the work, and maintain the file.
{{goalLine}}
End the run with `loopany report` — or, when this loop has a goal and you judge it met, `loopany finish`.
