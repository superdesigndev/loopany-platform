[loop run · {{name}}]

Run now. Read your task file at {{taskFile}} (create it from your Spec if it is
missing), carry out its `## Spec` against the current state, and maintain the file.
{{goalLine}}
End the run with `loopany report` — or, if this loop has a goal and you judge that
goal met, `loopany finish` (see your standing instructions).
