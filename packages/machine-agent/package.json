{
  "name": "@loopany/machine-agent",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "description": "Loopany machine agent \u2014 the machine side of the graph engine. Senses the outside world, executes approved outward-effect directives, and dispatches sandboxed runs, all with LOCAL credentials. The server never touches GitHub and never executes work.",
  "bin": {
    "loopany-agent": "dist/cli.js"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "start": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "4.1.9"
  }
}
